import { headers } from "next/headers"; import { redirect } from "next/navigation"; import { DepartmentManager } from "@/features/admin/components/department-manager"; import { getAdminDepartments } from "@/features/admin/repositories/admin-repository"; import { getServerSession } from "@/lib/server-session"
export default async function AdminDepartmentsPage() { const session = await getServerSession(await headers()); if (!session) redirect("/auth?mode=sign-in"); const data = await getAdminDepartments(session.user.id); if (!data) redirect("/onboarding"); return <main className="mx-auto w-full max-w-[1500px] p-4 sm:p-6 lg:p-8"><DepartmentManager departments={data.departments} headOptions={data.headOptions} /></main> }
